"""Evaluation harness for AI quality — ARB items #27 (RAG groundedness) and #28 (action correctness).

Run all evals:
    cd backend && python -m tests.eval.run_all

Run individually:
    cd backend && python -m tests.eval.eval_rag_groundedness
    cd backend && python -m tests.eval.eval_action_correctness
"""
