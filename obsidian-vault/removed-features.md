---
name: removed-features
description: Features deliberately removed from Centriq AI — do not re-add without explicit request
metadata: 
  node_type: memory
  type: project
  originSessionId: 57f36690-92cd-4141-a51e-59259061e208
---

## Security Digest (removed 2026-06-11)

Removed completely per user request. Was a daily cybersecurity news email digest (The Hacker News, Bleeping Computer, CISA KEV) with an admin UI to configure recipients, send hour, and news sources.

**Why:** User asked to remove it entirely — no stated reason, treat as deliberate product decision.

**How to apply:** Do not recreate any part of this: no sidebar entry, no route, no backend scheduler, no config vars. If a future request mentions "security digest" or "cybersecurity news digest", confirm before adding anything back.

**What was deleted:**
- `src/pages/SecurityDigestPage.tsx` (standalone admin page)
- `backend/app/routes/security_news_routes.py` (API routes)
- `backend/app/services/security_news_service.py` (RSS/CISA fetcher + config service)

**What was edited:**
- `src/components/Sidebar.tsx` — nav entry removed
- `src/routes/_layout.control-hub.tsx` — import, TabId, route, description removed
- `src/pages/ITPortal.tsx` — Tab type, default tab, tab button, rendered tab, full `SecurityDigestTab` component removed
- `backend/app/main.py` — router import, `include_router`, scheduler task removed
- `backend/app/config.py` — `SECURITY_NEWS_RECIPIENTS/SENDER/HOUR/ENABLED` removed
- `backend/app/services/email_service.py` — `send_security_news_digest()` removed

## Biweekly Project-Update form (removed 2026-06-18)

The "what are you working on?" feature: a scheduler that biweekly-emailed employees a form link, manager approval of submissions (via email/Teams approval tokens, entity_type="project_update"), and a PMO-portal tab to configure cadence/activity options + review submissions. Approved drafts wrote into employee_allocations.

**Why:** User asked to remove the whole feature ("remove project update asking form thing" → confirmed end-to-end). Considered redundant.

**How to apply:** Do not recreate any part. If a future request mentions "project update form" / "biweekly update", confirm before re-adding.

**What was deleted:** `backend/app/services/project_update_service.py`, `backend/app/routes/project_update_routes.py`, `src/routes/_layout.project-update.tsx`, model `ProjectUpdateSubmission`.
**What was edited:** `backend/app/main.py` (router import/include, scheduler `run_due` call, reject-label dict entry, `_finalize_decision` project_update branch), `backend/app/routes/pmo_portal_routes.py` (project-update endpoints + ProjectUpdateConfigBody), `backend/app/services/email_service.py` (`send_project_update_form_email`/`_approval_request`/`_decision_notification`), `backend/app/config.py` (`PROJECT_UPDATE_SENDER`), `src/pages/PMOPortal.tsx` (dropped the tab — only Udemy remains), plus copy in `agent.py` greeting + `access_routes.py`/`AccessManagement.tsx` PMO descriptions. routeTree.gen.ts auto-regenerated by the running TanStack dev server.
